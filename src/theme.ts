import { DefaultTheme } from 'styled-components';

const darkTheme: DefaultTheme = {
  bgColor: '#2f3640',
  textColor: '#f5f6fa',
  accentColor: '#959AEC',
};

const lightTheme = {
  bgColor: '#f5f6fa',
  textColor: '#2f3640',
  accentColor: '#3498db',
};

export { darkTheme, lightTheme };
